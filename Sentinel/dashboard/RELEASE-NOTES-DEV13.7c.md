# Sentinel Dev13.7c — Fixed-Size Zone Handles & Polygon UI Cleanup

- Polygon vertex handles stay a constant screen size while zooming.
- Polygon midpoint handles stay a constant screen size while zooming.
- Circle radius handle stays a constant screen size while zooming.
- Larger invisible hit targets keep handles easy to grab.
- Fixes the giant white handle seen at extreme zoom.
- Polygon radius/centre controls remain hidden for polygon geometry.
- Dev13.7b map rendering and zoom persistence are unchanged.
